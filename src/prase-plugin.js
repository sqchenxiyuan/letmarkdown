let pluginMap={};

class PrasePlugin{
    constructor(config){
        if(!config.name){
            console.error('未定义插件的名字');
            return null;
        }

        
    }

    check(text){

    }
}

export default PrasePlugin;